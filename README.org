This is a small, unpolished script that generates my ASCII and PDF resumes.
* Dependencies
- perl
- pdflatex
- texlive, with extra packages
- JSON, Text::Forge from CPAN
* Running
Just execute `go.pl`.
